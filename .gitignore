# === Reference clones (not part of this repo) ================================
/generator/

# === Local editor / agent config & crash dumps ===============================
.claude*
core
*.core

# === Bootstrapped tooling & vendored corelibs (fetched by scripts/bootstrap.sh)
/tools/
/vendor/

# === Per-language build outputs ==============================================
# Generated *source* (protoc / sofabgen output) IS committed so the repo is
# browsable and usable without a regen; only heavy derived build outputs are
# ignored — each languages/<lang>/.gitignore lists its own toolchain's
# outputs. setup.sh regenerates the source on demand.

# === Benchmark run artifacts =================================================
/results/raw/

# === Secrets =================================================================
.env
*.env
/scratchpad/
