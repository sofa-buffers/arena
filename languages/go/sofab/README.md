# Generated SofaBuffers Go module

`@generated by sofabgen`. Encode/decode harness over `corelib-go`.

```sh
# point the replace at a corelib-go checkout, then:
go run ./harness encode <Message> < in.json   # JSON -> bytes
go run ./harness decode <Message> < bytes.bin # bytes -> JSON
```
