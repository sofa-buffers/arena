// Code generated by sofabgen; DO NOT EDIT.
// JSON encode/decode harness: harness encode|decode <message> (stdin -> stdout).
const std = @import("std");
const message = @import("message.zig");

// -- JSON value readers (tolerant: a missing/mistyped member keeps the default)
fn jsonU64(v: std.json.Value) u64 {
    switch (v) {
        .integer => |i| return if (i < 0) 0 else @intCast(i),
        .float => |f| return if (f < 0) 0 else @intFromFloat(f),
        .number_string => |s| return std.fmt.parseInt(u64, s, 10) catch 0,
        else => return 0,
    }
}

fn jsonI64(v: std.json.Value) i64 {
    switch (v) {
        .integer => |i| return i,
        .float => |f| return @intFromFloat(f),
        .number_string => |s| return std.fmt.parseInt(i64, s, 10) catch 0,
        else => return 0,
    }
}

fn jsonF64(v: std.json.Value) f64 {
    switch (v) {
        .integer => |i| return @floatFromInt(i),
        .float => |f| return f,
        .number_string => |s| return std.fmt.parseFloat(f64, s) catch 0,
        else => return 0,
    }
}

fn jsonBool(v: std.json.Value) bool {
    return switch (v) {
        .bool => |b| b,
        else => false,
    };
}

fn jsonStr(v: std.json.Value) []const u8 {
    return switch (v) {
        .string => |s| s,
        else => "",
    };
}

// A blob is a JSON array of byte values.
fn jsonBlob(alloc: std.mem.Allocator, v: std.json.Value) []const u8 {
    switch (v) {
        .array => |a| {
            const s = alloc.alloc(u8, a.items.len) catch @panic("oom");
            for (a.items, 0..) |it, k| s[k] = @truncate(jsonU64(it));
            return s;
        },
        else => return "",
    }
}

fn jsonWriteBytes(b: []const u8, w: *std.Io.Writer) !void {
    try w.writeByte('[');
    for (b, 0..) |e, i| {
        if (i != 0) try w.writeByte(',');
        try w.print("{d}", .{e});
    }
    try w.writeByte(']');
}

fn toJson_ExampleArrays(o: *const message.ExampleArrays, w: *std.Io.Writer) std.Io.Writer.Error!void {
    try w.writeByte('{');
    try w.writeAll("\"u8\":");
    try w.writeByte('[');
    for (o.u8, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"i8\":");
    try w.writeByte('[');
    for (o.i8, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"u16\":");
    try w.writeByte('[');
    for (o.u16, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"i16\":");
    try w.writeByte('[');
    for (o.i16, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"u32\":");
    try w.writeByte('[');
    for (o.u32, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"i32\":");
    try w.writeByte('[');
    for (o.i32, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"u64\":");
    try w.writeByte('[');
    for (o.u64, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"i64\":");
    try w.writeByte('[');
    for (o.i64, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"nested\":");
    try toJson_ExampleArraysNested(&o.nested, w);
    try w.writeByte('}');
}

fn fromJson_ExampleArrays(alloc: std.mem.Allocator, v: std.json.Value) message.ExampleArrays {
    var o: message.ExampleArrays = .{};
    const obj = switch (v) {
        .object => |ob| ob,
        else => return o,
    };
    if (obj.get("u8")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.u8[k0] = @truncate(jsonU64(it0));
            }
        },
        else => {},
    };
    if (obj.get("i8")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.i8[k0] = @truncate(jsonI64(it0));
            }
        },
        else => {},
    };
    if (obj.get("u16")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.u16[k0] = @truncate(jsonU64(it0));
            }
        },
        else => {},
    };
    if (obj.get("i16")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.i16[k0] = @truncate(jsonI64(it0));
            }
        },
        else => {},
    };
    if (obj.get("u32")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.u32[k0] = @truncate(jsonU64(it0));
            }
        },
        else => {},
    };
    if (obj.get("i32")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.i32[k0] = @truncate(jsonI64(it0));
            }
        },
        else => {},
    };
    if (obj.get("u64")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.u64[k0] = jsonU64(it0);
            }
        },
        else => {},
    };
    if (obj.get("i64")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.i64[k0] = jsonI64(it0);
            }
        },
        else => {},
    };
    if (obj.get("nested")) |x| o.nested = fromJson_ExampleArraysNested(alloc, x);
    return o;
}

fn toJson_ExampleArraysNested(o: *const message.ExampleArraysNested, w: *std.Io.Writer) std.Io.Writer.Error!void {
    try w.writeByte('{');
    try w.writeAll("\"fp32\":");
    try w.writeByte('[');
    for (o.fp32, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeAll(",\"fp64\":");
    try w.writeByte('[');
    for (o.fp64, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try w.print("{d}", .{_e0});
    }
    try w.writeByte(']');
    try w.writeByte('}');
}

fn fromJson_ExampleArraysNested(alloc: std.mem.Allocator, v: std.json.Value) message.ExampleArraysNested {
    var o: message.ExampleArraysNested = .{};
    const obj = switch (v) {
        .object => |ob| ob,
        else => return o,
    };
    _ = alloc;
    if (obj.get("fp32")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.fp32[k0] = @floatCast(jsonF64(it0));
            }
        },
        else => {},
    };
    if (obj.get("fp64")) |x| switch (x) {
        .array => |a0| {
            for (a0.items, 0..) |it0, k0| {
                if (k0 >= 5) break;
                o.fp64[k0] = jsonF64(it0);
            }
        },
        else => {},
    };
    return o;
}

fn toJson_ExampleNested(o: *const message.ExampleNested, w: *std.Io.Writer) std.Io.Writer.Error!void {
    try w.writeByte('{');
    try w.writeAll("\"f32\":");
    try w.print("{d}", .{o.f32});
    try w.writeAll(",\"f64\":");
    try w.print("{d}", .{o.f64});
    try w.writeAll(",\"str\":");
    try std.json.Stringify.encodeJsonString(o.str, .{}, w);
    try w.writeAll(",\"bytes_field\":");
    try jsonWriteBytes(o.bytes_field, w);
    try w.writeByte('}');
}

fn fromJson_ExampleNested(alloc: std.mem.Allocator, v: std.json.Value) message.ExampleNested {
    var o: message.ExampleNested = .{};
    const obj = switch (v) {
        .object => |ob| ob,
        else => return o,
    };
    if (obj.get("f32")) |x| o.f32 = @floatCast(jsonF64(x));
    if (obj.get("f64")) |x| o.f64 = jsonF64(x);
    if (obj.get("str")) |x| o.str = jsonStr(x);
    if (obj.get("bytes_field")) |x| o.bytes_field = jsonBlob(alloc, x);
    return o;
}

fn toJson_Example(o: *const message.Example, w: *std.Io.Writer) std.Io.Writer.Error!void {
    try w.writeByte('{');
    try w.writeAll("\"u8\":");
    try w.print("{d}", .{o.u8});
    try w.writeAll(",\"i8\":");
    try w.print("{d}", .{o.i8});
    try w.writeAll(",\"u16\":");
    try w.print("{d}", .{o.u16});
    try w.writeAll(",\"i16\":");
    try w.print("{d}", .{o.i16});
    try w.writeAll(",\"u32\":");
    try w.print("{d}", .{o.u32});
    try w.writeAll(",\"i32\":");
    try w.print("{d}", .{o.i32});
    try w.writeAll(",\"u64\":");
    try w.print("{d}", .{o.u64});
    try w.writeAll(",\"i64\":");
    try w.print("{d}", .{o.i64});
    try w.writeAll(",\"nested\":");
    try toJson_ExampleNested(&o.nested, w);
    try w.writeAll(",\"arrays\":");
    try toJson_ExampleArrays(&o.arrays, w);
    try w.writeAll(",\"string_array\":");
    try w.writeByte('[');
    for (o.string_array, 0..) |_e0, _i0| {
        if (_i0 != 0) try w.writeByte(',');
        try std.json.Stringify.encodeJsonString(_e0, .{}, w);
    }
    try w.writeByte(']');
    try w.writeByte('}');
}

fn fromJson_Example(alloc: std.mem.Allocator, v: std.json.Value) message.Example {
    var o: message.Example = .{};
    const obj = switch (v) {
        .object => |ob| ob,
        else => return o,
    };
    if (obj.get("u8")) |x| o.u8 = @truncate(jsonU64(x));
    if (obj.get("i8")) |x| o.i8 = @truncate(jsonI64(x));
    if (obj.get("u16")) |x| o.u16 = @truncate(jsonU64(x));
    if (obj.get("i16")) |x| o.i16 = @truncate(jsonI64(x));
    if (obj.get("u32")) |x| o.u32 = @truncate(jsonU64(x));
    if (obj.get("i32")) |x| o.i32 = @truncate(jsonI64(x));
    if (obj.get("u64")) |x| o.u64 = jsonU64(x);
    if (obj.get("i64")) |x| o.i64 = jsonI64(x);
    if (obj.get("nested")) |x| o.nested = fromJson_ExampleNested(alloc, x);
    if (obj.get("arrays")) |x| o.arrays = fromJson_ExampleArrays(alloc, x);
    if (obj.get("string_array")) |x| switch (x) {
        .array => |a0| {
            const s0 = alloc.alloc([]const u8, a0.items.len) catch @panic("oom");
            for (a0.items, 0..) |it0, k0| s0[k0] = jsonStr(it0);
            o.string_array = s0;
        },
        else => {},
    };
    return o;
}

var _bench_example_in: message.Example = undefined;
var _bench_example_wire: []const u8 = &[_]u8{};
var _bench_example_alloc: std.mem.Allocator = undefined;

export fn run_encode_example() void {
    _bench_example_wire = _bench_example_in.encode(_bench_example_alloc) catch &[_]u8{};
    std.mem.doNotOptimizeAway(&_bench_example_wire);
}

export fn run_decode_example() void {
    const obj = message.Example.decode(_bench_example_alloc, _bench_example_wire) catch return;
    std.mem.doNotOptimizeAway(&obj);
}

// benchMain runs one op of <workload>. Everything here is setup; only the
// run_* call is collected.
fn benchMain(alloc: std.mem.Allocator, w: []const u8, input: []const u8) !void {
    if (std.mem.eql(u8, w, "encode_example") or std.mem.eql(u8, w, "decode_example")) {
        const v = try std.json.parseFromSliceLeaky(std.json.Value, alloc, input, .{});
        _bench_example_alloc = alloc;
        _bench_example_in = fromJson_Example(alloc, v);
        if (std.mem.eql(u8, w, "encode_example")) {
            @call(.never_inline, run_encode_example, .{});
        } else {
            @call(.never_inline, run_encode_example, .{}); // setup: the decode input (not collected)
            @call(.never_inline, run_decode_example, .{});
        }
        std.debug.print("BYTES={d}\n", .{_bench_example_wire.len});
        return;
    }
    std.process.exit(2);
}

pub fn main(init: std.process.Init) !void {
    const alloc = init.arena.allocator();
    var args = std.process.Args.Iterator.init(init.minimal.args);
    _ = args.next();
    const mode = args.next() orelse "";
    const name = args.next() orelse "example";

    var rbuf: [4096]u8 = undefined;
    var stdin = std.Io.File.stdin().reader(init.io, &rbuf);
    const input = try stdin.interface.allocRemaining(alloc, .unlimited);
    var wbuf: [4096]u8 = undefined;
    var stdout = std.Io.File.stdout().writer(init.io, &wbuf);
    const out = &stdout.interface;

    // `bench <workload>` takes a workload, not a message name.
    if (std.mem.eql(u8, mode, "bench")) {
        try benchMain(alloc, name, input);
        return;
    }

    if (std.mem.eql(u8, name, "example")) {
        if (std.mem.eql(u8, mode, "encode")) {
            const v = try std.json.parseFromSliceLeaky(std.json.Value, alloc, input, .{});
            const obj = fromJson_Example(alloc, v);
            try out.writeAll(try obj.encode(alloc));
        } else if (std.mem.eql(u8, mode, "decode")) {
            const obj = try message.Example.decode(alloc, input);
            try toJson_Example(&obj, out);
            try out.writeByte('\n');
        } else {
            std.process.exit(2);
        }
        try out.flush();
        return;
    }
    std.process.exit(2);
}
