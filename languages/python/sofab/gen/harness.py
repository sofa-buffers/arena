# Code generated by sofabgen; DO NOT EDIT.
import sys, json
import message

MESSAGES = {
    "example": message.Example,
}

def main() -> int:
    if len(sys.argv) < 2:
        sys.stderr.write('usage: harness.py <encode|decode> [Message]\n')
        return 2
    mode = sys.argv[1]
    name = sys.argv[2] if len(sys.argv) > 2 else "example"
    cls = MESSAGES.get(name)
    if cls is None:
        sys.stderr.write('unknown message\n')
        return 2
    data = sys.stdin.buffer.read()
    if mode == 'encode':
        obj = cls.from_jsonable(json.loads(data))
        sys.stdout.buffer.write(obj.encode())
    elif mode == 'decode':
        obj = cls.decode(data)
        sys.stdout.write(json.dumps(obj.to_jsonable()))
        sys.stdout.write('\n')
    else:
        sys.stderr.write('unknown mode\n')
        return 2
    return 0

if __name__ == '__main__':
    sys.exit(main())
