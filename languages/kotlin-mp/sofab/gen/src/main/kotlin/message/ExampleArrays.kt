// Code generated by sofabgen; DO NOT EDIT.
@file:OptIn(ExperimentalUnsignedTypes::class)
@file:Suppress("DEPRECATION", "RedundantVisibilityModifier", "unused", "UNUSED_PARAMETER", "RemoveRedundantCallsOfConversionMethods", "KotlinConstantConditions")

package message

import org.sofabuffers.sofab.*

public class ExampleArrays {
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var u8: UByteArray = Sbuf.EMPTY_UBYTE
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var i8: ByteArray = Sbuf.EMPTY_BYTE
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var u16: UShortArray = Sbuf.EMPTY_USHORT
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var i16: ShortArray = Sbuf.EMPTY_SHORT
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var u32: UIntArray = Sbuf.EMPTY_UINT
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var i32: IntArray = Sbuf.EMPTY_INT
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var u64: ULongArray = Sbuf.EMPTY_ULONG
    /** Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. */
    public var i64: LongArray = Sbuf.EMPTY_LONG
    public var nested: ExampleArraysNested = ExampleArraysNested()

    /** Write this object's fields into [os]. Streaming out: nothing is flushed -- see [encodeTo]. */
    public fun serialize(os: OStream) {
        if (this.u8.isNotEmpty()) {
            os.writeArrayUnsigned(0, this.u8.asByteArray())
        }
        if (this.i8.isNotEmpty()) {
            os.writeArraySigned(1, this.i8)
        }
        if (this.u16.isNotEmpty()) {
            os.writeArrayUnsigned(2, this.u16.asShortArray())
        }
        if (this.i16.isNotEmpty()) {
            os.writeArraySigned(3, this.i16)
        }
        if (this.u32.isNotEmpty()) {
            os.writeArrayUnsigned(4, this.u32.asIntArray())
        }
        if (this.i32.isNotEmpty()) {
            os.writeArraySigned(5, this.i32)
        }
        if (this.u64.isNotEmpty()) {
            os.writeArrayUnsigned(6, this.u64.asLongArray())
        }
        if (this.i64.isNotEmpty()) {
            os.writeArraySigned(7, this.i64)
        }
        os.writeSequenceBeginLazy(10); this.nested.serialize(os); os.writeSequenceEnd()
    }

    /** True when every field still equals its declared default, compared per field and recursively -- i.e. serialize would write nothing at all. */
    internal fun isDefault(): Boolean {
        if (this.u8.isNotEmpty()) return false
        if (this.i8.isNotEmpty()) return false
        if (this.u16.isNotEmpty()) return false
        if (this.i16.isNotEmpty()) return false
        if (this.u32.isNotEmpty()) return false
        if (this.i32.isNotEmpty()) return false
        if (this.u64.isNotEmpty()) return false
        if (this.i64.isNotEmpty()) return false
        if (!this.nested.isDefault()) return false
        return true
    }

    /** Restore every field to its declared default, in place; call before reusing an instance as a decode destination. */
    public fun reset() {
        this.u8 = Sbuf.EMPTY_UBYTE
        this.i8 = Sbuf.EMPTY_BYTE
        this.u16 = Sbuf.EMPTY_USHORT
        this.i16 = Sbuf.EMPTY_SHORT
        this.u32 = Sbuf.EMPTY_UINT
        this.i32 = Sbuf.EMPTY_INT
        this.u64 = Sbuf.EMPTY_ULONG
        this.i64 = Sbuf.EMPTY_LONG
        this.nested.reset()
    }
}

