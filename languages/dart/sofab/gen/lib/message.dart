// Code generated by sofabgen; DO NOT EDIT.
// ignore_for_file: unused_field, unused_element, deprecated_member_use_from_same_package
import 'dart:typed_data';
import 'package:sofabuffers/sofabuffers.dart' as sofab;

// Widen the 32 raw wire bits of an fp32 NaN to a display double for element
// access; the exact bits are kept alongside for a bit-for-bit re-encode.
double _f32FromBits(int bits) =>
    (ByteData(4)..setUint32(0, bits, Endian.little)).getFloat32(0, Endian.little);

class ExampleArrays {
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> u8 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> i8 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> u16 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> i16 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> u32 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> i32 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> u64 = <int>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<int> i64 = <int>[];
  ExampleArraysNested nested = ExampleArraysNested();

  void serialize(sofab.Encoder e) {
    if (u8.isNotEmpty) { e.writeUnsignedArray(0, u8); }
    if (i8.isNotEmpty) { e.writeSignedArray(1, i8); }
    if (u16.isNotEmpty) { e.writeUnsignedArray(2, u16); }
    if (i16.isNotEmpty) { e.writeSignedArray(3, i16); }
    if (u32.isNotEmpty) { e.writeUnsignedArray(4, u32); }
    if (i32.isNotEmpty) { e.writeSignedArray(5, i32); }
    if (u64.isNotEmpty) { e.writeUnsignedArray(6, u64); }
    if (i64.isNotEmpty) { e.writeSignedArray(7, i64); }
    e.beginSequenceLazy(10); nested.serialize(e); e.endSequence();
  }

  /// Restores every field to its declared default, in place.
  ///
  /// A field is only written during decode when the wire carries it, and a
  /// field equal to its default is not on the wire at all, so a destination
  /// must start from the defaults for an absent field to read back as its
  /// default. [tryDecode] does that for you; call this directly when driving
  /// the decode visitor yourself, or to recycle an instance.
  ///
  /// Lists are cleared and refilled rather than replaced, so a reused
  /// instance keeps its backing storage. (A list member assigned a
  /// fixed-length list by the caller is the one exception -- see the fp32
  /// array note in the generator docs.)
  void reset() {
    u8.clear();
    i8.clear();
    u16.clear();
    i16.clear();
    u32.clear();
    i32.clear();
    u64.clear();
    i64.clear();
    nested.reset();
  }

  /// Whether every field equals its declared default, compared per field and
  /// recursively -- i.e. whether [serialize] would write no child at all.
  bool get _isDefault {
    if (!(u8.isEmpty)) return false;
    if (!(i8.isEmpty)) return false;
    if (!(u16.isEmpty)) return false;
    if (!(i16.isEmpty)) return false;
    if (!(u32.isEmpty)) return false;
    if (!(i32.isEmpty)) return false;
    if (!(u64.isEmpty)) return false;
    if (!(i64.isEmpty)) return false;
    if (!(nested._isDefault)) return false;
    return true;
  }
}

class _ExampleArraysVisitor extends sofab.VisitorBase {
  _ExampleArraysVisitor(this.o);
  final ExampleArrays o;
  @override
  void onUnsignedArray(int id, Int64List values) {
    switch (id) {
      case 0:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < 0 || _v > 255) { invalidate(); return; } }
        o.u8 = List<int>.from(values);
        return;
      case 2:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < 0 || _v > 65535) { invalidate(); return; } }
        o.u16 = List<int>.from(values);
        return;
      case 4:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < 0 || _v > 4294967295) { invalidate(); return; } }
        o.u32 = List<int>.from(values);
        return;
      case 6:
        if (values.length > 5) { invalidate(); return; }
        o.u64 = List<int>.from(values);
        return;
    }
  }
  @override
  void onSignedArray(int id, Int64List values) {
    switch (id) {
      case 1:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < -128 || _v > 127) { invalidate(); return; } }
        o.i8 = List<int>.from(values);
        return;
      case 3:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < -32768 || _v > 32767) { invalidate(); return; } }
        o.i16 = List<int>.from(values);
        return;
      case 5:
        if (values.length > 5) { invalidate(); return; }
        for (final _v in values) { if (_v < -2147483648 || _v > 2147483647) { invalidate(); return; } }
        o.i32 = List<int>.from(values);
        return;
      case 7:
        if (values.length > 5) { invalidate(); return; }
        o.i64 = List<int>.from(values);
        return;
    }
  }
  @override
  void onArrayBegin(int id, sofab.ArrayKind kind, int count) {
    switch (id) {
      case 0:
        if (kind == sofab.ArrayKind.unsigned && count > 5) invalidate();
        return;
      case 1:
        if (kind == sofab.ArrayKind.signed && count > 5) invalidate();
        return;
      case 2:
        if (kind == sofab.ArrayKind.unsigned && count > 5) invalidate();
        return;
      case 3:
        if (kind == sofab.ArrayKind.signed && count > 5) invalidate();
        return;
      case 4:
        if (kind == sofab.ArrayKind.unsigned && count > 5) invalidate();
        return;
      case 5:
        if (kind == sofab.ArrayKind.signed && count > 5) invalidate();
        return;
      case 6:
        if (kind == sofab.ArrayKind.unsigned && count > 5) invalidate();
        return;
      case 7:
        if (kind == sofab.ArrayKind.signed && count > 5) invalidate();
        return;
    }
  }
  @override
  sofab.ElemRange? onArrayElemBound(int id, sofab.ArrayKind kind) {
    switch (id) {
      case 0:
        if (kind == sofab.ArrayKind.unsigned) {
          return const sofab.ElemRange(0, 255);
        }
        return null;
      case 1:
        if (kind == sofab.ArrayKind.signed) {
          return const sofab.ElemRange(-128, 127);
        }
        return null;
      case 2:
        if (kind == sofab.ArrayKind.unsigned) {
          return const sofab.ElemRange(0, 65535);
        }
        return null;
      case 3:
        if (kind == sofab.ArrayKind.signed) {
          return const sofab.ElemRange(-32768, 32767);
        }
        return null;
      case 4:
        if (kind == sofab.ArrayKind.unsigned) {
          return const sofab.ElemRange(0, 4294967295);
        }
        return null;
      case 5:
        if (kind == sofab.ArrayKind.signed) {
          return const sofab.ElemRange(-2147483648, 2147483647);
        }
        return null;
    }
    return null;
  }
  @override
  sofab.MessageVisitor? onSequenceStart(int id) {
    switch (id) {
      case 10:
        return _ExampleArraysNestedVisitor(o.nested);
    }
    return null;
  }
}

class ExampleArraysNested {
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<double> fp32 = <double>[];
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated.
  List<double> fp64 = <double>[];

  void serialize(sofab.Encoder e) {
    if (fp32.isNotEmpty) { e.writeFp32Array(0, fp32); }
    if (fp64.isNotEmpty) { e.writeFp64Array(1, fp64); }
  }

  /// Restores every field to its declared default, in place.
  ///
  /// A field is only written during decode when the wire carries it, and a
  /// field equal to its default is not on the wire at all, so a destination
  /// must start from the defaults for an absent field to read back as its
  /// default. [tryDecode] does that for you; call this directly when driving
  /// the decode visitor yourself, or to recycle an instance.
  ///
  /// Lists are cleared and refilled rather than replaced, so a reused
  /// instance keeps its backing storage. (A list member assigned a
  /// fixed-length list by the caller is the one exception -- see the fp32
  /// array note in the generator docs.)
  void reset() {
    fp32 = <double>[];
    fp64.clear();
  }

  /// Whether every field equals its declared default, compared per field and
  /// recursively -- i.e. whether [serialize] would write no child at all.
  bool get _isDefault {
    if (!(fp32.isEmpty)) return false;
    if (!(fp64.isEmpty)) return false;
    return true;
  }
}

class _ExampleArraysNestedVisitor extends sofab.VisitorBase {
  _ExampleArraysNestedVisitor(this.o);
  final ExampleArraysNested o;
  @override
  void onFp32Array(int id, Float32List values) {
    switch (id) {
      case 0:
        if (values.length > 5) { invalidate(); return; }
        o.fp32 = sofab.copyFp32(values, values.length);
        return;
    }
  }
  @override
  void onFp64Array(int id, Float64List values) {
    switch (id) {
      case 1:
        if (values.length > 5) { invalidate(); return; }
        o.fp64 = List<double>.from(values);
        return;
    }
  }
  @override
  void onArrayBegin(int id, sofab.ArrayKind kind, int count) {
    switch (id) {
      case 0:
        if (kind == sofab.ArrayKind.fp32 && count > 5) invalidate();
        return;
      case 1:
        if (kind == sofab.ArrayKind.fp64 && count > 5) invalidate();
        return;
    }
  }
  @override
  sofab.MessageVisitor? onSequenceStart(int id) {
    return null;
  }
}

class ExampleNested {
  double f32 = 0.0;
  int? f32Fp32Bits;
  double f64 = 0.0;
  /// Schema bound: maxlen 32 -- a longer value is INVALID, never truncated.
  String str = '';
  /// Schema bound: maxlen 4 -- a longer value is INVALID, never truncated.
  Uint8List bytes_field = Uint8List(0);

  void serialize(sofab.Encoder e) {
    if (f32 != 0.0) {
      if (f32.isNaN && f32Fp32Bits != null) { e.writeFp32Bits(0, f32Fp32Bits!); } else { e.writeFp32(0, f32); }
    }
    if (f64 != 0.0) { e.writeFp64(1, f64); }
    if (str != '') { e.writeString(2, str); }
    if (bytes_field.isNotEmpty) { e.writeBlob(3, bytes_field); }
  }

  /// Restores every field to its declared default, in place.
  ///
  /// A field is only written during decode when the wire carries it, and a
  /// field equal to its default is not on the wire at all, so a destination
  /// must start from the defaults for an absent field to read back as its
  /// default. [tryDecode] does that for you; call this directly when driving
  /// the decode visitor yourself, or to recycle an instance.
  ///
  /// Lists are cleared and refilled rather than replaced, so a reused
  /// instance keeps its backing storage. (A list member assigned a
  /// fixed-length list by the caller is the one exception -- see the fp32
  /// array note in the generator docs.)
  void reset() {
    f32 = 0.0;
    f32Fp32Bits = null;
    f64 = 0.0;
    str = '';
    bytes_field = Uint8List(0);
  }

  /// Whether every field equals its declared default, compared per field and
  /// recursively -- i.e. whether [serialize] would write no child at all.
  bool get _isDefault {
    if (!(f32 == 0.0)) return false;
    if (!(f64 == 0.0)) return false;
    if (!(str == '')) return false;
    if (!(bytes_field.isEmpty)) return false;
    return true;
  }
}

class _ExampleNestedVisitor extends sofab.VisitorBase {
  _ExampleNestedVisitor(this.o);
  final ExampleNested o;
  @override
  void onFp32(int id, double value) {
    switch (id) {
      case 0:
        o.f32 = value;
        o.f32Fp32Bits = null;
        return;
    }
  }
  @override
  void onFp32Bits(int id, int bits) {
    switch (id) {
      case 0:
        o.f32Fp32Bits = bits;
        o.f32 = _f32FromBits(bits);
        return;
    }
  }
  @override
  void onFp64(int id, double value) {
    switch (id) {
      case 1:
        o.f64 = value;
        return;
    }
  }
  @override
  void onStringBytes(int id, Uint8List bytes) {
    switch (id) {
      case 2:
        if (bytes.length > 32) { invalidate(); return; }
        {
          final s = sofab.decodeUtf8Strict(bytes);
          if (s == null) { invalidate(); return; }
          o.str = s;
        }
        return;
    }
  }
  @override
  void onBlob(int id, Uint8List value) {
    switch (id) {
      case 3:
        if (value.length > 4) { invalidate(); return; }
        o.bytes_field = Uint8List.fromList(value);
        return;
    }
  }
  @override
  void onFixlenHeader(int id, int subtype, int length) {
    switch (id) {
      case 2:
        if (subtype == sofab.FixlenType.string && length > 32) invalidate();
        return;
      case 3:
        if (subtype == sofab.FixlenType.blob && length > 4) invalidate();
        return;
    }
  }
  @override
  sofab.MessageVisitor? onSequenceStart(int id) {
    return null;
  }
}

/// This example demonstrates the use of SofaBuffers to encode and decode a message.
class Example {
  int u8 = 0;
  int i8 = 0;
  int u16 = 0;
  int i16 = 0;
  int u32 = 0;
  int i32 = 0;
  int u64 = 0;
  int i64 = 0;
  ExampleNested nested = ExampleNested();
  ExampleArrays arrays = ExampleArrays();
  /// Schema bound: count 5 is a CAPACITY, not a length -- starts empty; over 5 elements is INVALID, never truncated. Element maxlen 64, same rule.
  List<String> string_array = <String>[];

  void serialize(sofab.Encoder e) {
    if (u8 != 0) { e.writeUnsigned(0, u8); }
    if (i8 != 0) { e.writeSigned(1, i8); }
    if (u16 != 0) { e.writeUnsigned(2, u16); }
    if (i16 != 0) { e.writeSigned(3, i16); }
    if (u32 != 0) { e.writeUnsigned(4, u32); }
    if (i32 != 0) { e.writeSigned(5, i32); }
    if (u64 != 0) { e.writeUnsigned(6, u64); }
    if (i64 != 0) { e.writeSigned(7, i64); }
    e.beginSequenceLazy(10); nested.serialize(e); e.endSequence();
    e.beginSequenceLazy(100); arrays.serialize(e); e.endSequence();
    e.beginSequenceLazy(200);
    for (var _i0 = 0; _i0 < string_array.length; _i0++) { if (string_array[_i0].isNotEmpty || _i0 == string_array.length - 1) e.writeString(_i0, string_array[_i0]); }
    e.endSequence();
  }

  /// Restores every field to its declared default, in place.
  ///
  /// A field is only written during decode when the wire carries it, and a
  /// field equal to its default is not on the wire at all, so a destination
  /// must start from the defaults for an absent field to read back as its
  /// default. [tryDecode] does that for you; call this directly when driving
  /// the decode visitor yourself, or to recycle an instance.
  ///
  /// Lists are cleared and refilled rather than replaced, so a reused
  /// instance keeps its backing storage. (A list member assigned a
  /// fixed-length list by the caller is the one exception -- see the fp32
  /// array note in the generator docs.)
  void reset() {
    u8 = 0;
    i8 = 0;
    u16 = 0;
    i16 = 0;
    u32 = 0;
    i32 = 0;
    u64 = 0;
    i64 = 0;
    nested.reset();
    arrays.reset();
    string_array.clear();
  }

  /// Whether every field equals its declared default, compared per field and
  /// recursively -- i.e. whether [serialize] would write no child at all.
  bool get _isDefault {
    if (!(u8 == 0)) return false;
    if (!(i8 == 0)) return false;
    if (!(u16 == 0)) return false;
    if (!(i16 == 0)) return false;
    if (!(u32 == 0)) return false;
    if (!(i32 == 0)) return false;
    if (!(u64 == 0)) return false;
    if (!(i64 == 0)) return false;
    if (!(nested._isDefault)) return false;
    if (!(arrays._isDefault)) return false;
    if (!(string_array.isEmpty)) return false;
    return true;
  }

  /// Worst-case serialized size, derived from the schema: no value of this
  /// message can encode to more, which is why [encode] can size one exact
  /// buffer from it.
  static const int maxSize = 732;
  /// Serializes this message into a buffer this call allocates and owns.
  ///
  /// The buffer is exactly [maxSize] bytes -- the schema's worst case -- so any
  /// conformant value fits. A value filled past a declared count/maxlen does
  /// not, and throws [sofab.SofabException] (`bufferFull`) rather than being
  /// handed back truncated.
  Uint8List encode() {
    final buf = Uint8List(maxSize);
    final e = sofab.Encoder.overBuffer(buf);
    serialize(e);
    e.flush();
    return e.written;
  }

  /// Encodes into an [sofab.Encoder] the caller owns, then flushes the tail.
  ///
  /// The encoder's buffer may be smaller than the message: it is drained
  /// through the flush callback as it fills, so what bounds memory is the
  /// buffer, not the message.
  void encodeTo(sofab.Encoder e) {
    serialize(e);
    e.flush();
  }

  /// Status-surfacing one-shot decode: fills [out] and
  /// returns the terminal decode outcome. `invalid` covers both malformed
  /// bytes and a schema-bound violation (over-count/over-index/over-maxlen);
  /// `incomplete` means the bytes end inside a field or an open sequence.
  ///
  /// [out] is REUSABLE: it is [reset] to the declared defaults first. That
  /// reset is what makes a reused destination correct: a field equal to its
  /// default is not written to the wire at all, nested objects and arrays
  /// included, so nothing in the bytes can clear a value left over from an
  /// earlier decode.
  static sofab.DecodeStatus tryDecode(Uint8List data, Example out) {
    out.reset();
    return _decodeInto(data, out);
  }

  /// Decodes into a destination the caller guarantees is already at its
  /// defaults, so [decode]'s fresh instance skips the redundant reset.
  static sofab.DecodeStatus _decodeInto(Uint8List data, Example out) {
    return sofab.Decoder.decode(data, _ExampleVisitor(out));
  }

  /// Best-effort one-shot decode (the 90 % case): returns the message with
  /// every field decoded so far, discarding the status. Prefer [tryDecode]
  /// when a truncated or malformed message must be distinguished.
  static Example decode(Uint8List data) {
    final m = Example();
    _decodeInto(data, m);
    return m;
  }

  /// An incremental decoder filling [out]: hold it and feed chunks as they
  /// arrive, instead of buffering the whole message first.
  ///
  /// [out] is [reset] first, for the reason [tryDecode] resets: an absent
  /// field fires no callback, so a value left over from an earlier decode
  /// would survive.
  static ExampleDecoder decoder(Example out) {
    out.reset();
    return ExampleDecoder._(out);
  }
}

/// Incremental decoder for [Example]: hold one and feed the message as
/// bytes arrive, instead of buffering it whole first.
///
/// The wire format has no end marker at the top level -- a message ends
/// where its bytes end -- so a feed cannot report that the MESSAGE is
/// complete, only that the bytes handed in ended on a field boundary
/// (`complete`) or mid-field (`incomplete`). Neither is a failure
/// mid-stream; the caller's own framing decides when the input is over, and
/// [finish] then gives the verdict for the message as a whole.
///
/// Nothing is borrowed from the chunks you feed: the corelib copies each
/// string/blob payload into storage of its own before it reaches the
/// destination, so a chunk may be reused as soon as [feed] returns.
class ExampleDecoder {
  ExampleDecoder._(this._out) {
    _d = sofab.Decoder(_ExampleVisitor(_out));
  }

  final Example _out;
  late final sofab.Decoder _d;
  sofab.DecodeStatus _st = sofab.DecodeStatus.complete;

  /// Feeds the next chunk, of any size. `complete` means the bytes ended on
  /// a field boundary, `incomplete` mid-field -- neither answers whether the
  /// MESSAGE is done. `invalid` is terminal.
  sofab.DecodeStatus feed(List<int> chunk) {
    _st = _d.feed(chunk);
    return status;
  }

  /// The outcome for everything fed so far, without feeding more.
  sofab.DecodeStatus get status => _st;

  /// The destination, holding whatever has been decoded so far.
  Example get message => _out;

  /// Takes the decoded message once the caller's framing says the input is
  /// over. Returns null if the stream ended mid-field or was rejected, so a
  /// half-filled value is never mistaken for a whole one; read [status] for
  /// which it was, or [message] to get it anyway.
  Example? finish() =>
      status == sofab.DecodeStatus.complete ? _out : null;
}

class _ExampleVisitor extends sofab.VisitorBase {
  _ExampleVisitor(this.o);
  final Example o;
  @override
  void onUnsigned(int id, int value) {
    switch (id) {
      case 0:
        if (value < 0 || value > 255) { invalidate(); return; }
        o.u8 = value;
        return;
      case 2:
        if (value < 0 || value > 65535) { invalidate(); return; }
        o.u16 = value;
        return;
      case 4:
        if (value < 0 || value > 4294967295) { invalidate(); return; }
        o.u32 = value;
        return;
      case 6:
        o.u64 = value;
        return;
    }
  }
  @override
  void onSigned(int id, int value) {
    switch (id) {
      case 1:
        if (value < -128 || value > 127) { invalidate(); return; }
        o.i8 = value;
        return;
      case 3:
        if (value < -32768 || value > 32767) { invalidate(); return; }
        o.i16 = value;
        return;
      case 5:
        if (value < -2147483648 || value > 2147483647) { invalidate(); return; }
        o.i32 = value;
        return;
      case 7:
        o.i64 = value;
        return;
    }
  }
  @override
  sofab.MessageVisitor? onSequenceStart(int id) {
    switch (id) {
      case 10:
        return _ExampleNestedVisitor(o.nested);
      case 100:
        return _ExampleArraysVisitor(o.arrays);
      case 200:
        o.string_array = <String>[];
        return sofab.StringSeq(o.string_array, 5, 64);
    }
    return null;
  }
}

