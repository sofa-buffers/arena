// Code generated by sofabgen; DO NOT EDIT.
// ignore_for_file: unused_local_variable, deprecated_member_use_from_same_package
import 'dart:convert';
import 'dart:io';
import 'dart:typed_data';
import 'package:harness/message.dart';
import 'package:sofa_buffers_corelib/sofa_buffers_corelib.dart' as sofab;

int _exact64(Object? v) {
  if (v is int) return v;
  if (v is double && v == v.roundToDouble() && v.abs() <= 9007199254740992.0) {
    return v.toInt();
  }
  throw FormatException(
      'a 64-bit field arrived as the JSON number $v, which jsonDecode has already '
      'rounded -- spell a value above 2^53 as a string so it survives');
}

Map<String, dynamic> _toJsonExampleArrays(ExampleArrays m) {
  return <String, dynamic>{
    'u8': m.u8.toList(),
    'i8': m.i8.toList(),
    'u16': m.u16.toList(),
    'i16': m.i16.toList(),
    'u32': m.u32.toList(),
    'i32': m.i32.toList(),
    'u64': [for (var _k = 0; _k < m.u64.length; _k++) BigInt.from(m.u64.storage[_k]).toUnsigned(64).toString()],
    'i64': m.i64.toList(),
    'nested': _toJsonExampleArraysNested(m.nested),
  };
}

ExampleArrays _fromJsonExampleArrays(Map<String, dynamic> j) {
  final m = ExampleArrays();
  if (j.containsKey('u8')) { m.u8.assign(<int>[for (final _b in (j['u8'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('i8')) { m.i8.assign(<int>[for (final _b in (j['i8'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('u16')) { m.u16.assign(<int>[for (final _b in (j['u16'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('i16')) { m.i16.assign(<int>[for (final _b in (j['i16'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('u32')) { m.u32.assign(<int>[for (final _b in (j['u32'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('i32')) { m.i32.assign(<int>[for (final _b in (j['i32'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('u64')) { m.u64.assign(<int>[for (final _b in (j['u64'] as List)) (_b is String ? BigInt.parse(_b) : BigInt.from(_exact64(_b))).toSigned(64).toInt()]); }
  if (j.containsKey('i64')) { m.i64.assign(<int>[for (final _b in (j['i64'] as List)) (_b as num).toInt()]); }
  if (j.containsKey('nested')) { m.nested = _fromJsonExampleArraysNested(j['nested'] as Map<String, dynamic>); }
  return m;
}

Map<String, dynamic> _toJsonExampleArraysNested(ExampleArraysNested m) {
  return <String, dynamic>{
    'fp32': m.fp32.toList(),
    'fp64': m.fp64.toList(),
  };
}

ExampleArraysNested _fromJsonExampleArraysNested(Map<String, dynamic> j) {
  final m = ExampleArraysNested();
  if (j.containsKey('fp32')) { m.fp32.assign(<double>[for (final _b in (j['fp32'] as List)) (_b as num).toDouble()]); }
  if (j.containsKey('fp64')) { m.fp64.assign(<double>[for (final _b in (j['fp64'] as List)) (_b as num).toDouble()]); }
  return m;
}

Map<String, dynamic> _toJsonExampleNested(ExampleNested m) {
  return <String, dynamic>{
    'f32': m.f32,
    'f64': m.f64,
    'str': m.str.toString(),
    'bytes_field': m.bytes_field.toBytes().toList(),
  };
}

ExampleNested _fromJsonExampleNested(Map<String, dynamic> j) {
  final m = ExampleNested();
  if (j.containsKey('f32')) { m.f32 = (j['f32'] as num).toDouble(); }
  if (j.containsKey('f64')) { m.f64 = (j['f64'] as num).toDouble(); }
  if (j.containsKey('str')) { m.str.assignString(j['str'] as String); }
  if (j.containsKey('bytes_field')) { m.bytes_field.assign(<int>[for (final _b in (j['bytes_field'] as List)) (_b as num).toInt()]); }
  return m;
}

Map<String, dynamic> _toJsonExample(Example m) {
  return <String, dynamic>{
    'u8': m.u8,
    'i8': m.i8,
    'u16': m.u16,
    'i16': m.i16,
    'u32': m.u32,
    'i32': m.i32,
    'u64': BigInt.from(m.u64).toUnsigned(64).toString(),
    'i64': m.i64,
    'nested': _toJsonExampleNested(m.nested),
    'arrays': _toJsonExampleArrays(m.arrays),
    'string_array': [for (final _x in m.string_array) _x.toString()],
  };
}

Example _fromJsonExample(Map<String, dynamic> j) {
  final m = Example();
  if (j.containsKey('u8')) { m.u8 = (j['u8'] as num).toInt(); }
  if (j.containsKey('i8')) { m.i8 = (j['i8'] as num).toInt(); }
  if (j.containsKey('u16')) { m.u16 = (j['u16'] as num).toInt(); }
  if (j.containsKey('i16')) { m.i16 = (j['i16'] as num).toInt(); }
  if (j.containsKey('u32')) { m.u32 = (j['u32'] as num).toInt(); }
  if (j.containsKey('i32')) { m.i32 = (j['i32'] as num).toInt(); }
  if (j.containsKey('u64')) { m.u64 = (j['u64'] is String ? BigInt.parse(j['u64'] as String) : BigInt.from(_exact64(j['u64']))).toSigned(64).toInt(); }
  if (j.containsKey('i64')) { m.i64 = (j['i64'] as num).toInt(); }
  if (j.containsKey('nested')) { m.nested = _fromJsonExampleNested(j['nested'] as Map<String, dynamic>); }
  if (j.containsKey('arrays')) { m.arrays = _fromJsonExampleArrays(j['arrays'] as Map<String, dynamic>); }
  if (j.containsKey('string_array')) { m.string_array = <sofab.InlineString>[for (final _x in (j['string_array'] as List)) sofab.InlineString.of(_x as String)]; }
  return m;
}

int _benchSink = 0;

int _benchMain(String w, int reps, Uint8List input) {
  final warmup = int.tryParse(Platform.environment['SOFAB_BENCH_WARMUP'] ?? '') ?? 5000;
  if (w == 'encode_example' || w == 'decode_example') {
    final obj = _fromJsonExample(jsonDecode(utf8.decode(input)) as Map<String, dynamic>);
    final wire = obj.encode();
    final enc = w == 'encode_example';
    for (var i = 0; i < warmup; i++) { _benchOpExample(enc, obj, wire); }
    for (var i = 0; i < reps; i++) { _benchOpExample(enc, obj, wire); }
    stderr.writeln('sink=$_benchSink bytes=${wire.length}');
    return 0;
  }
  stderr.writeln('unknown workload: $w');
  return 2;
}

void _benchOpExample(bool enc, Example obj, Uint8List wire) {
  if (enc) {
    _benchSink ^= obj.encode().length;
  } else {
    _benchSink ^= Example.decode(wire).u8;
  }
}

void main(List<String> args) {
  if (args.isEmpty) {
    stderr.writeln('usage: harness <encode|decode|streamdecode|trydecode|recode|bench> [Message|workload]');
    exit(2);
  }
  final mode = args[0];
  final name = args.length > 1 ? args[1] : 'example';
  final input = _readStdin();
  if (mode == 'bench') {
    exit(_benchMain(name, args.length > 2 ? int.parse(args[2]) : 1000, input));
  }
  switch (name) {
    case 'example':
      if (mode == 'encode') {
        final obj = _fromJsonExample(jsonDecode(utf8.decode(input)) as Map<String, dynamic>);
        stdout.add(obj.encode());
      } else if (mode == 'decode') {
        final obj = Example();
        final st = Example.tryDecode(input, obj);
        if (st != sofab.DecodeStatus.complete) {
          stderr.writeln('decode failed: ${st.name}');
          exit(1);
        }
        stdout.writeln(jsonEncode(_toJsonExample(obj)));
      } else if (mode == 'streamdecode') {
        final out = Example();
        final dec = Example.decoder(out);
        final csz = args.length > 2 ? int.parse(args[2]) : 1;
        final step = csz > 0 ? csz : (input.isEmpty ? 1 : input.length);
        for (var off = 0; off < input.length; off += step) {
          final end = off + step < input.length ? off + step : input.length;
          final st = dec.feed(input.sublist(off, end));
          if (st != sofab.DecodeStatus.complete &&
              st != sofab.DecodeStatus.incomplete) {
            final re = dec.feed(const <int>[]);
            final fin = dec.finish() == null ? 'null' : 'RETURNED';
            stderr.writeln(
                'decode failed: ${st.name} [refeed=${re.name}] [finish=$fin]');
            exit(1);
          }
        }
        final obj = dec.finish();
        if (obj == null) {
          stderr.writeln('decode failed: ${dec.feed(const <int>[]).name}');
          exit(1);
        }
        stdout.writeln(jsonEncode(_toJsonExample(obj)));
      } else if (mode == 'trydecode') {
        final obj = Example();
        final st = Example.tryDecode(input, obj);
        stdout.writeln(st.name.toUpperCase());
        stdout.writeln(jsonEncode(_toJsonExample(obj)));
      } else if (mode == 'recode') {
        final obj = Example();
        final st = Example.tryDecode(input, obj);
        if (st != sofab.DecodeStatus.complete) {
          stderr.writeln('decode failed: ${st.name}');
          exit(1);
        }
        stdout.add(obj.encode());
      } else {
        stderr.writeln('unknown mode');
        exit(2);
      }
      return;
    default:
      stderr.writeln('unknown message');
      exit(2);
  }
}

Uint8List _readStdin() {
  final b = BytesBuilder(copy: true);
  var byte = stdin.readByteSync();
  while (byte != -1) {
    b.addByte(byte);
    byte = stdin.readByteSync();
  }
  return b.toBytes();
}
